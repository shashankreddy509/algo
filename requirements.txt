# Core Flask dependencies
Flask==3.0.0
Werkzeug==3.0.1

# API and HTTP clients
requests==2.31.0
fyers-apiv3>=3.1.0,<4.0.0

# Environment and configuration
python-dotenv==1.0.0

# Firebase integration
firebase-admin==6.4.0

# WSGI server
gunicorn==21.2.0

# Build tools (install first)
setuptools>=68.0.0
wheel>=0.41.0

# aiohttp installation strategies (uncomment one that works)
# Strategy 1: Try pre-compiled wheels first (recommended)
# --only-binary=all aiohttp

# Strategy 2: Use older stable version if compilation fails
# aiohttp==3.8.6

# Strategy 3: Install dependencies separately then aiohttp
# yarl>=1.9.0
# multidict>=6.0.0
# async-timeout>=4.0.0
# aiosignal>=1.3.0
# frozenlist>=1.3.0
# attrs>=22.0.0
# aiohttp

# Strategy 4: Default (let pip handle it)
# aiohttp

# Note: aiohttp is a dependency of fyers-apiv3, so it will be installed automatically
# If you encounter build errors, uncomment one of the strategies above